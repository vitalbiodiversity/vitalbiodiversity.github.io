title: Vital Biodiversity
theme: jekyll-theme-minimal

# Sidebar navigation
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
      sidebar: true

# Optional: remove theme credit in footer
minimal:
  footer: ""
